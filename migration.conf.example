################################################################################
# ZCS to ZCS Migration Configuration
#
# Copy this file to migration.conf and fill in your server details
# cp migration.conf.example migration.conf
################################################################################

# Source Server Configuration
SOURCE_SERVER="source.example.com"          # Source ZCS server hostname or IP
SOURCE_SSH_USER="root"                       # SSH user for source server

# Destination Server Configuration
DEST_SERVER="destination.example.com"       # Destination ZCS server hostname or IP
DEST_SSH_USER="root"                         # SSH user for destination server

# Migration Settings
DEFAULT_PASSWORD="ChangeMe123!"              # Default password for migrated accounts
                                             # Users should change this after migration

# Version Compatibility
IGNORE_VERSION_MISMATCH=false                # Set to true to proceed even if versions don't match
                                             # WARNING: May cause data inconsistencies

# Mailbox Sync Settings
SKIP_MAILBOX_SYNC=false                      # Set to true to skip mailbox data synchronization
DRY_RUN=false                                # Set to true to perform a dry run without actual sync

# Directory Settings
LOG_DIR="./logs"                             # Directory for log files
MIGRATION_DATA_DIR="./migration_data"        # Directory for migration data files

# Advanced Settings (Optional)
# RSYNC_OPTIONS="-avz --progress"            # Additional rsync options
# EXCLUDE_ACCOUNTS=""                         # Comma-separated list of accounts to exclude
# EXCLUDE_DOMAINS=""                          # Comma-separated list of domains to exclude

################################################################################
# Notes:
#
# 1. SSH Key Authentication:
#    - Ensure SSH key-based authentication is set up between the source
#      and destination servers for passwordless access
#    - Test SSH connection before running the migration
#
# 2. ZCS Versions:
#    - Both servers MUST run the same ZCS version for successful migration
#    - You can check the version with: zmcontrol -v
#
# 3. Disk Space:
#    - Ensure destination server has sufficient disk space
#    - At least 2x the size of source mailbox data is recommended
#
# 4. Network:
#    - Stable network connection is required
#    - Consider using screen or tmux for long-running migrations
#
# 5. Backup:
#    - Always backup both servers before starting migration
#    - Test the migration process in a non-production environment first
#
# 6. Downtime:
#    - Plan for appropriate downtime window
#    - Notify users in advance
#    - Consider running initial sync before final migration to reduce downtime
################################################################################
